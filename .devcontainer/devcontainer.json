{
  "dockerComposeFile": "./docker-compose.yml",
  "service": "app",
  "postCreateCommand": "pip install -r requirements.txt",
  "workspaceFolder": "/django-oidc-test",
  "extensions": [
    "ms-python.python"
  ],
  "settings": {
    "python.formatting.provider": "black"
  }
}
